# Database

Gereserveerd voor het Drizzle-schema, SQL-migrations en gecontroleerde seeddata voor PostgreSQL.

Productieschemawijzigingen verlopen altijd via gereviewde en gecommitte migrations; nooit via een rechtstreekse schema-push.
